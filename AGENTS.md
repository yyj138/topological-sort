# 项目协作要求

## 项目与范围

本项目统一使用 https://github.com/yyj138/topological-sort，不再按旧方案中的 Gitee 地址创建第二个仓库。开发 Java + Swing 桌面应用，数据使用文本文件，不能改为网页项目。遵守任务书和团队分工，只修改当前任务需要的文件，不替其他成员虚构完成记录。

## Git 与工作记录

用户已在本项目会话中授权：完成确认范围内的实际工作后，通过本地 Git 命令提交并推送到自己的工作分支，供团队查看阶段成果。当前 A 的工作分支为 `dev-a`；如果用户后续要求先确认或只做检查，应优先遵循。使用现有、已核实的作者配置，不伪造作者、日期、会议或工作量，不为了增加提交数制造无意义变更。

提交前检查状态与差异，仅暂存本次相关文件，执行与变更相适应的验证。提交说明采用 `【任务编号】具体说明`，推送后核对本地与远程提交是否一致，并向用户报告成果、验证结果和下一步。未经用户明确要求，不直接改写 `main`，不创建或合并拉取请求，不强制推送，不改写已发布的历史。

## 个人日志

只有当用户明确表示结束当天工作并要求整理个人日志时，才根据实际工作生成简短 Markdown 日志。日志保存于仓库之外的用户本地目录，仅用于个人总结和阶段汇报，不上传 GitHub，也不把私人日志内容放进公开工作记录。只记录能够核实的工作；用户自行完成或线下讨论的内容，需要用户补充后再写入。

## 技术与验证

当前工程使用 JDK 21 编译，跨模块接口处于团队评审阶段。模型和算法不依赖 Swing；GUI 由 B、C 负责，耗时计算与界面更新分工应在接口中明确。对算法和解析的实质变更运行正确性及相关边界验证；对文档和简单可逆改动只做必要检查，避免为形式上的覆盖编写无意义测试。
