# A—B 需求口径对齐说明

更新日期：2026 年 9 月 19 日。状态：**三项均已确认并同步完毕**。本文档保留作为历史记录，后续如无新分歧不再维护。

## 一、枚举默认上限：已统一为 10000 条 / 30 秒

| 比对位置 | 最终内容 |
| --- | --- |
| A：接口契约 §4 | `maxResults` 由调用方传入，0 表示不限数量；B 侧默认 10000 |
| B：MainController.java | `MAX_RESULTS = 10000`，`TIMEOUT_MILLIS = 30_000L` |
| B：需求分析报告 §3.2 | 调用配置上限 10000 条、超时 30 秒 |
| B：需求分析报告附录 Q-2 | 10000 条，超时 30 秒，可取消 |
| B：README | 枚举上限 10000 条 |

**结论**：B 侧采用 10000 条 / 30 秒 / 可取消，A 契约保持参数化接口不变。无"全部切换"按钮（枚举被 10000 条上限截断时状态栏提示"达到上限"）。

## 二、图 1 节选：已与 figure1.txt 对齐

B 的需求报告 §5.3 图 1 节选已替换为与 `data/figure1.txt` 一致的 6 条边，节点名保留内部空格（如 `MA 140` 而非 `MA140`）：

```
<MA 140,MA 141>
<MA 141,CS 150>
<MA 141,CS 225>
<CS 150,CS 155>
<CS 155,CS 200>
<CS 155,CS 225>
```

**结论**：节选、Mermaid 图和实际数据文件三处一致。15 节点 / 16 边，由 D 提供，A 实测验证通过。

## 三、自环措辞：已统一

B 的需求报告 §5.1 第 8 条已替换为：

> 自环 `<a,a>` 保留在图中并作为环报告；含自环的输入图不存在合法的完整拓扑排序，界面提示并高亮自环，不得通过忽略自环继续生成完整排序结果。

**结论**：与 A 契约一致。自环在 Graph 中保留，CycleDetector 返回 `[a,a]`，B 侧弹窗提示，C 侧画布高亮。

## 参考

- B 侧：`dev-b` 分支 `docs/需求分析报告.md`、`src/ui/MainController.java`
- A 侧：`dev-a` 分支 `docs/接口契约.md`、`data/figure1.txt`
