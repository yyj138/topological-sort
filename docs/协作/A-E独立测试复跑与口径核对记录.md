# A—E 独立测试复跑与口径核对记录

核查批次：2026 年 9 月 20 日；整理日期：2026 年 9 月 22 日。E 编写独立算法测试、解析容错测试及命令行入口，A 在独立目录组合 A/D/E 的固定版本进行复跑，并核对结果、性能条件和覆盖边界。本文记录的是 A 的集成复现与测试口径核查工作，不将 E 的测试用例开发计入 A，也不将会前待确认事项写成 E 已回复或团队已决议。

## 固定版本与复跑方法

| 部分 | 固定提交 |
| --- | --- |
| A 模型与算法 | `a65627183104e8c2b7aa913772aaf9b0a26a4b1d` |
| D 解析与文件模块 | `c9d99e8a0d249caa177e81f5711ff0716e571644` |
| E 测试与运行入口 | `0ed294a1a01a9a5ed3178879a73b8b81c82fe93a` |

A 将上述源码放入独立临时目录，执行 E 的 AlgorithmTest、ParserFaultTest 和命令行入口，并分别保留控制台及结果文件。这样核查的不只是 E 已上传日志是否写着通过，还包括选定 A/D 代码组合能否实际复现。复跑未直接修改成员分支，结果也没有覆盖 E 的原上传批次。

## 结果与性能条件核对

| 内容 | 留存结果 | 适用范围 |
| --- | --- | --- |
| AlgorithmTest | 18 项通过，0 失败 | 算法独立测试，包括小图、图 1、空图孤立点、环、结果语义和千节点随机图 |
| ParserFaultTest 主解析用例 | 29 项通过，1 项 SKIP，0 失败 | SKIP 为前层已拦截输入、Graph 兜底分支未触发，不能算作该分支通过 |
| 解析与预检一致性 | 15 项通过，0 失败 | 对同批输入比较 DataParser 与 InputValidator |
| 千节点随机 DAG | 固定种子 42，1000 节点、1241 条边，上限 1000 条，TIMEOUT 参数为 0；A 复跑枚举耗时 20 ms，LIMIT_REACHED | E 原上传批次记录 19 ms，两个数值分别保留；限量返回不等于完整枚举 |

A 检查了性能数字的统计范围：算法日志中的毫秒值不包括 GUI 绘图和文件导出，0 ms 只表示低于所用计时分辨率。上述性能用例主要断言数量上限与完整性，没有自动判定“必须在某个毫秒门槛内完成”。该批 E 用例未独立覆盖运行中取消和模拟超时，已有 A 作者测试应独立引用，不能在报告中改写成 E 已执行同样的检查。

## 衔接工作与后续边界

A 还核对了测试、运行说明与接口基准的对应关系，指出原 readme 中 JDK 和接口版本措辞需要按实际集成条件确认，并在会前材料中列明应补充的提交号、环境和命令信息。对 GUI 中途取消、连续计算、状态清理和导出，A 列出了与 B/C/D/E 协同检查的事项；这些是已提出的核查要求，并非所有场景已有独立测试通过记录。正式测试报告由 D 依据实际证据编写，A 提供算法语义、作者记录和组合复跑依据，E 保留测试开发及执行归属。

## 留存依据

[跨模块联调原始输出.txt](跨模块联调原始输出.txt) 分别收录 E 原上传算法日志的本地副本、A/D/E 组合复跑的算法与解析日志，每段标注原文件标识和 SHA-256。完整源码组合、命令行入口输出及会前核查说明保存在 A 的本地 `会议核查-20260920` 材料中，没有把整套临时副本上传为 A 的新增源码。本记录用于证明实际复现和边界核查，后续验收仍应固定最终集成版本。
