# A1 图结构实现与验证记录

整理说明（2026 年 9 月 22 日）：本文件独立保留 A1 的原始实现说明、用例覆盖和验证结论，正文日期及阶段状态沿用历史记录。目录和输出引用已更新，原控制台输出集中在 [作者自测原始输出.txt](作者自测原始输出.txt) 的 A1 段，历史内容未删减。后续跨模块验证另见 `docs/协作/`，不回写为本次作者自测已完成的事项。

2026 年 9 月 18 日，A1 的 Vertex、Edge、Graph 已完成本地实现，并通过作者自测。实现依据为 V1.0《接口契约》《代码规范》《图数据结构设计》，目标环境为 Windows、JDK 21.0.6、UTF-8。本记录只证明图结构的基础能力；算法重复计算及取消后不改变原图的集成验证，待 A2 至 A4 实现后执行，不代表 E 的独立测试或全组联调已完成。

## 交付文件与调用方式

| 文件 | 用途 |
| --- | --- |
| `src/model/Vertex.java` | 固定名称及前驱、后继集合；不向外暴露可变邻接集合 |
| `src/model/Edge.java` | 不可变有向边；按起点与终点判断相等，并提供一致的 hashCode |
| `src/model/Graph.java` | 实现契约中的九个实例方法，统一维护邻接关系及边数 |
| `test/GraphSelfTest.java` | A1 作者自测，含设计小图的可运行调用示例，无第三方依赖 |
| `docs/验证/作者自测原始输出.txt` | 实际执行自测后捕获的 UTF-8 纯文本输出，现集中保存于合并 TXT 的 A1 段 |

在仓库根目录执行以下 PowerShell 命令即可复现。编译采用 JDK 21，并开启全部编译警告且将警告视为错误；编译失败时应停止，不运行旧的 class 文件。`out/` 已由仓库现有规则忽略，不属于源码交付。

```powershell
javac -encoding UTF-8 --release 21 -Xlint:all -Werror -d out/a1 src/model/Vertex.java src/model/Edge.java src/model/Graph.java test/GraphSelfTest.java
if ($LASTEXITCODE -ne 0) { throw 'A1 编译失败' }
java -cp out/a1 GraphSelfTest
if ($LASTEXITCODE -ne 0) { throw 'A1 自测失败' }
```

初次在临时目录编译时出现 class 文件写入错误，未进入测试阶段。将相同源码写入实际仓库后，以获准的本地执行权限编译成功，未修改源码来掩盖环境错误。成功编译时额外传入 `-J-Duser.language=en` 以便读取编译诊断；它不改变程序逻辑，编译无警告，编译和自测进程退出码均为 0。

## 关键方法与边界

| 方法或行为 | 实现说明 |
| --- | --- |
| `addVertex` | 去重但不覆盖已有节点及其关系，新增返回 true |
| `addEdge` | 先验证两个端点，再按起点、终点顺序补齐缺失节点；新增关系同时维护后继、前驱与边数 |
| `removeEdge` | 两端存在且有边时同步删除关系并减少边数；边不存在返回 false；任一端点不存在抛出 IllegalArgumentException；不删除节点 |
| 名称校验 | 共用同一校验函数；使用 String.strip() 去除首尾空白，保留内部空格并区分大小写；拒绝 null、空名称、`<`、`>`、`,`、`＜`、`＞` 及换行（CR、LF、NEL、Unicode 行／段分隔符） |
| 节点和后继查询 | 返回按录入顺序排列的不可修改快照；再次查询可获得最新数据，旧列表不会随图变化 |
| 度数与统计 | 入度取前驱集合大小，出度取后继集合大小；图中维护去重边数；自环计一条边并分别贡献一个入度、出度 |
| 未知节点与非法参数 | 抛出带原因的 IllegalArgumentException，拒绝操作时图保持不变；不输出界面提示 |
| 存储与模块边界 | LinkedHashMap 保存节点，LinkedHashSet 保存直接邻接关系；不保存独立全图边列表，不添加间接边，不依赖 Swing 或文件解析 |

图的空间规模为 O(V+E)。忽略名称长度且按哈希表平均复杂度计，单次节点／边增删和度数查询为 O(1)，读取节点列表为 O(V)，读取某节点的后继快照为 O(该节点出度)。Graph 是可变且非线程安全的结构，调用方应在后台计算期间保证输入稳定。

## 实际自测输出

原始输出现保存在 [作者自测原始输出.txt 的 A1 段](作者自测原始输出.txt)，内容与下方输出一致。本次提交前使用 D:\jdk21 中的 javac 和 java 重新编译、执行自测后生成该文件，结果仍为 16/16 组通过。

以下内容来自实际仓库中的 `java -cp out/a1 GraphSelfTest`。自测使用显式校验并在失败时抛出 AssertionError，不依赖 `-ea`。每组基础用例独立建图；最后一组以固定种子 20260918 产生 500 次增删操作，每次均与独立的 8×8 布尔邻接矩阵对照全部关系、各节点入出度、节点数和总边数。

```text
PASS 1: empty graph
PASS 2: isolated vertex
PASS 3: automatic endpoints
PASS 4: duplicate vertex preserves edges
PASS 5: duplicate edge preserves degrees
PASS 6: self-loop add, deduplicate and remove
PASS 7: opposite directed edges
Example: vertices=[A, B, C, D, E], edges=3, inDegree(C)=2
  A -> [C]
  B -> [C]
  C -> [D]
  D -> []
  E -> []
After removing A -> C: vertices=5, edges=2, inDegree(C)=1
PASS 8: design example and deletion
PASS 9: missing edge and repeated deletion
PASS 10: names: whitespace, Chinese, internal spaces and case
PASS 11: invalid names and atomic rejection
PASS 12: unknown node queries and deletion
PASS 13: read-only snapshots
PASS 14: stable insertion order
PASS 15: Vertex and directed Edge value semantics
PASS 16: 500 seeded mutations against an adjacency matrix
PASS: 16/16 cases
```

## 后续对接边界

C 可通过节点列表与后继列表遍历并绘图，D 可通过 addVertex/addEdge 建图，B 和 E 可直接使用同一个 model.Graph。A2 至 A4 尚未在本任务中实现，正式图 1 的排序验证及解析器、界面联调也不在本次通过范围内。没有新增删除节点、节点改名或公开前驱查询等额外接口；已有 A5 契约及设计文档保持原样。

## 补充：A1—A4 统一自测入口（2026 年 9 月 22 日）


在仓库根目录使用 JDK 21 执行以下 PowerShell 命令，`javac` 和 `java` 应来自同一套 JDK。编译仅选择 A 的模型、算法和作者测试，开启 UTF-8、`--release 21`、`-Xlint:all` 和 `-Werror`，避免将其他成员的 GUI 警告混入 A 模块严格编译结论。编译和运行失败时立即停止，产物与复跑输出写入已被忽略的 `out/a-self-test/`。

```powershell
$aSources = @(Get-ChildItem -LiteralPath 'src/model','src/algorithm' -Filter '*.java' |
    ForEach-Object FullName)
$aSources += @(
    'test/GraphSelfTest.java',
    'test/TopologicalSolverSelfTest.java',
    'test/CycleDetectorSelfTest.java',
    'test/algorithm/AllTopoSortsSelfTest.java',
    'test/AlgorithmSelfTestSuite.java'
)
javac -encoding UTF-8 --release 21 -Xlint:all -Werror -d out/a-self-test/classes @aSources
if ($LASTEXITCODE -ne 0) { throw 'A 模块严格编译失败' }
java '-Dstdout.encoding=UTF-8' '-Dstderr.encoding=UTF-8' -cp out/a-self-test/classes AlgorithmSelfTestSuite |
    Tee-Object -FilePath out/a-self-test/运行输出.txt
if ($LASTEXITCODE -ne 0) { throw 'A 模块自测失败' }
```

不传参数时，统一入口读取 `data/figure1.txt`；需要验证同一图 1 的其他文件副本时，可以在类名后传入该文件路径。新生成的全量结果固定写入 `out/a-self-test/all_results.txt`，该入口不接受历史输出文件作为写入目标。四个原自测程序使用显式断言检查，不依赖 JVM 的 `-ea` 参数；A2 单独运行不带输入时仅执行 18 组，A3 不带两项文件参数时仅执行 24 组，统一入口始终包含图 1。

此次新增统一入口已在整理副本中通过严格编译和全部 79 组作者自测，31,185 条结果与历史文件逐条一致。统一入口不替代各任务的独立验证记录，也不合并计算各批次测试数量。
