========================================
  FileManager 文件读写测试
========================================

  [PASS] 往返读写一致性（半角输入）
         → txt内容:
           # 拓扑排序结果

           # 共 2 条序列

           # 是否完整：是

           

           序列 1: MA 140 -> MA 141 -> CS 150 -> CS 155

           序列 2: MA 140 -> MA 141 -> CS 225 -> CS 230

  [PASS] 导出txt格式
         → csv内容:
           ﻿# 共 2 条序列

           # 是否完整：是

           序号,拓扑排序序列

           1,MA 140 -> MA 141 -> CS 150

           2,MA 140 -> MA 141 -> CS 225

  [PASS] 导出csv格式（含BOM）
  [PASS] 读取不存在的文件抛异常
  [PASS] 传入null文件抛异常
  [PASS] 保存到null路径抛异常
  [PASS] 导出空结果抛异常
         → 最近路径: E:\topological-sort\test_path.txt
         → 最近目录: E:\topological-sort
  [PASS] 最近打开路径记录
  [PASS] 全角输入保存后读回为半角
  [PASS] 保存时换行统一为\n
         → txt内容（不完整）:
           # 拓扑排序结果

           # 共 2 条序列

           # 是否完整：否

           # 停止原因：达到输出上限，仅显示前 1000 条

           

           序列 1: MA 140 -> MA 141 -> CS 150

           序列 2: MA 140 -> MA 141 -> CS 225

  [PASS] 导出txt含完整性和停止原因（不完整）
         → txt内容（完整）:
           # 拓扑排序结果

           # 共 1 条序列

           # 是否完整：是

           

           序列 1: MA 140 -> MA 141 -> CS 150

  [PASS] 导出txt含完整性和停止原因（完整）
         → csv内容（不完整）:
           ﻿# 共 1 条序列

           # 是否完整：否

           # 停止原因：用户取消

           序号,拓扑排序序列

           1,MA 140 -> MA 141 -> CS 150

  [PASS] 导出csv含完整性和停止原因（不完整）
         → csv内容（完整）:
           ﻿# 共 1 条序列

           # 是否完整：是

           序号,拓扑排序序列

           1,MA 140 -> MA 141 -> CS 150

  [PASS] 导出csv含完整性和停止原因（完整）

========================================
  测试结果汇总：通过 14，失败 0
========================================
全部通过！
